import { redirectTo } from '../utils/redirect'
import Header from '../components/header'

const Profile = () => {

  return (
    <div>
      <Header theme/>
      <h1>dashboard</h1>
    </div>
  );
};

export default Profile;