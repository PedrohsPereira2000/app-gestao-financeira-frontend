import { redirectTo } from '../utils/redirect'
import Header from '../module/header'

const Dashboard = () => {

  return (
    <div>
      <Header theme/>
      <h1>dashboard</h1>
    </div>
  );
};

export default Dashboard;